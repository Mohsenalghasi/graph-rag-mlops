import re

class TextCleaner:
    """Light cleaning only. Do not destroy technical tags/IDs."""

    def clean(self, text: str) -> str:
        text = text.replace("\x00", " ")
        text = re.sub(r"[ \t]+", " ", text)
        text = re.sub(r"\n{3,}", "\n\n", text)
        return text.strip()
